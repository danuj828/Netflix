import React from "react";
import MovieCard from "./MovieCard";

const MovieList = () => {
  return (
    <div>
      <MovieCard />
      <MovieCard />
      <MovieCard />
    </div>
  );
};

export default MovieList;
